# kevinkho.com

## Deploy

This site deploys via **Render**, not GitHub Pages. Pushing to `master` triggers a Render build automatically. Do not use `docusaurus deploy` or create `gh-pages` branches.
